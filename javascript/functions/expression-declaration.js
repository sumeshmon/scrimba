// function declaration
// function getSpendAlert (amount){
//     return `The amount spend is ${amount}`
// }


// function expression
const getSpendAlert = function(amount){
    return `The amount spend is ${amount}`
}
console.log(getSpendAlert(200));